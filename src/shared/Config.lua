-- ReplicatedStorage/Config.lua
local RunService = game:GetService("RunService") -- чат гбт это вставил но я не вижу применения если к концу будет работать без него - удали

return {
	DEBUG = {
		CLAIM_BUTTON = true,        -- ← одним флипом выключаем
	},
}
